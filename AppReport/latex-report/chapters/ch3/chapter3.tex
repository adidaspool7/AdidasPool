\chapter{Analysis and Methodology}
\label{cap:analysis}

This chapter turns the problem stated in \chapref{cap:introduction} into
an analysed, prioritised body of work. It first characterises the
stakeholders and the two personas TalentHub serves, then revisits the
client pain points and the instrument used to validate them, sets out the
language talent framework that the client brief explicitly asked for,
derives the functional and non-functional requirements that follow from
those pains, and finally describes the project-management method.

%----------------------------------------------------------------------------------------

\section{Stakeholders and Personas}
\label{sec:analysis-stakeholders}

Four groups of stakeholders shaped the work. The \textbf{client} --- the HR
and Talent Acquisition team at adidas GBS Porto --- supplied the problem,
validated the pain points, and acted as acceptance tester through recurring
review sessions. The \textbf{end users} fall into two roles that the
application treats as first-class personas. The \textbf{academic
supervisors}, Prof.\ Nuno Escudeiro and Prof.\ Ana Cl\'audia Rodrigues,
provided methodological guidance and assessed the work in its academic
context. The \textbf{development team} that
built TalentHub was a two-person engineering team (\secref{sec:team})
that carried both engineering and, in the author's case, product ownership.

Two personas were used to keep design decisions grounded:

\begin{description}
	\item[Carla, the recruiter (HR persona).] An HR specialist at adidas
	GBS Porto who simultaneously owns a number of open roles and triages a
	high weekly volume of CVs. She needs to find, compare and contact
	candidates quickly, to know whether someone has applied before, and to
	justify shortlisting decisions to colleagues. She is the primary user
	of the HR dashboard, the matching screens, the notification tools and
	the analytics.

	\item[Tiago, the candidate (candidate persona).] A multilingual
	jobseeker who applies through the public portal, uploads his own CV,
	completes language or skill assessments when invited, and wishes to be
	kept informed of status changes rather than disappearing into a black
	hole. He is the primary user of the candidate self-service portal.
\end{description}

The two personas justify the role-based split that runs through the whole
system: the same application exposes different navigation, permissions and
screens depending on whether the authenticated user holds the HR or the
candidate role.

%----------------------------------------------------------------------------------------

\section{Pain Points and Survey Validation}
\label{sec:analysis-pains}

The nine pain points introduced in \tabref{tab:pain-points-overview} were
not assumed; they were elicited in discovery sessions, written up in a
client-facing document, and then probed through a fourteen-item survey in
which \emph{each pain point is covered by at least one question} (several
pains contributing more than one, to probe a single friction from
complementary angles). This keeps the later
validation in \chapref{cap:validation} traceable: a question that
scores well is direct evidence that the corresponding pain is real and that
the feature built to address it is wanted. The instrument was completed by the
lead HR stakeholder at adidas GBS Porto, so its results are treated as a
single-respondent indicator --- a constraint analysed in full when the
responses are examined in \chapref{cap:validation}. \tabref{tab:survey-mapping}
shows the mapping for the questions most relevant to the platform's core.

\begin{table}[ht]
\centering
\caption[Survey questions mapped to pain points and features]{Representative
survey questions, each mapped to a pain point and to the feature it
validates.}
\label{tab:survey-mapping}
\small
\begin{tabular}{p{0.30\linewidth}p{0.30\linewidth}p{0.30\linewidth}}
\toprule
\textbf{Survey question (theme)} & \textbf{Pain point} & \textbf{Feature validated} \\
\midrule
Hours/week spent reviewing CVs manually & \#5 manual CV extraction & AI CV parsing + job-fit scoring \\
Confidence in verifying language proficiency before interview & \#3 no first-pass screening & CEFR language assessment \\
Frequency of inconsistent candidate scoring across the team & \#7 no data-driven focus & Structured scoring + templates \\
Ease of retrieving full interaction history for a candidate & \#4 no structured contact channel & Interaction-history panel \\
Identifying in-demand skills and sharing pipeline insight with stakeholders & \#7 no data-driven focus & Analytics dashboards + custom widgets \\
\bottomrule
\end{tabular}
\end{table}

On the basis of discovery, four pain points were treated as the priority
core for the first releases: manual CV extraction (\#5), the absence of a
first-pass language and skill screen (\#3), keeping promising-but-rejected
candidates engaged (\#9), and recognising repeat applicants under the GDPR
retention rule (\#2). These four drove the ordering of the product backlog
(\secref{sec:analysis-pm}). The full survey instrument and its raw
responses are reproduced in \appref{app:survey}, and the analysis of
the results is presented as part of validation in
\secref{sec:val-survey}.

%----------------------------------------------------------------------------------------

\section{The Language Talent Framework}
\label{sec:analysis-framework}

The client brief did not ask only for a tool; its first stated objective was
to \emph{``design a language talent framework that defines the critical
language profiles required across adidas Porto's service areas and
establishes a structured process for attracting candidates who meet them''}.
The framework is therefore a deliverable in its own right, and the software
documented in the rest of this report is the instrument that operationalises
it. This section sets out the framework; the chapters that follow show how
each of its pillars is realised in code.

\begin{quote}
\emph{Note on sourcing.} The service-area list and the broad observation that
adidas GBS Porto operates across roughly fifteen business languages with an
international workforce of about 22\% come directly from the client brief.
The finer-grained assignment of \emph{which} languages are critical to
\emph{which} service area is a reasoned synthesis by the author, informed by
the brief and by the public structure of a European shared-services centre;
it is presented as a defensible model to be calibrated with the client, not
as an audited inventory of adidas's internal staffing data.
\end{quote}

\subsection{Critical Language Profiles by Service Area}
\label{sec:framework-profiles}

adidas Business in Portugal delivers shared services --- Finance, Human
Resources, Accounts and Sales Management, Supply Chain, Digital Services and
Tech --- to other adidas markets across Europe. Each service area inherits the
language needs of the markets it serves, which is what makes language a
\emph{role requirement} rather than a generic nice-to-have. English functions
as the common operating language across every area; the additional ``business
languages'' attach to the markets a team supports.
\tabref{tab:language-profiles} captures this mapping as the framework's
core artefact: a per-area profile of the language capabilities that make a
candidate valuable, expressed in the same CEFR vocabulary the assessment
module produces.

\begin{table}[ht]
\centering
\caption[Critical language profiles by service area]{Critical language
profiles by service area (illustrative synthesis from the client brief); CEFR
bands indicate the working level at which a language becomes an asset.}
\label{tab:language-profiles}
\small
\begin{tabular}{p{0.22\linewidth}p{0.40\linewidth}p{0.28\linewidth}}
\toprule
\textbf{Service area} & \textbf{Typically critical languages} & \textbf{Indicative working level} \\
\midrule
Finance & German, French, Italian, Spanish & B2--C1 (written + spoken) \\
Human Resources & German, Dutch, Polish, English & C1 (high spoken interaction) \\
Accounts \& Sales Mgmt. & French, Italian, Spanish, Nordic languages & B2--C1 (customer-facing) \\
Supply Chain & German, Dutch, Czech, Polish & B1--B2 (operational) \\
Digital Services & English plus one European language & B2 \\
Tech & English (primary), German (secondary) & B2 \\
\bottomrule
\end{tabular}
\end{table}

Two properties of this model matter for the design. First, the relevant level
is rarely native fluency: most roles need a \emph{working} business level
(B1--C1), which is exactly the band a structured, CEFR-aligned assessment can
place a candidate into with confidence, whereas self-declared ``fluent /
intermediate / basic'' labels cannot. Second, the profile is a property of
the \emph{role}, not of the candidate in the abstract --- which is precisely
why matching in TalentHub is job-anchored (\chapref{cap:design}): a
candidate's languages are scored against the profile a given opening
requires, not against a universal yardstick.

\subsection{The Three Pillars: Attract, Develop, Retain}
\label{sec:framework-pillars}

The brief framed the strategy as three obligations --- to \emph{attract},
\emph{develop} and \emph{retain} language capability sustainably.
\tabref{tab:framework-pillars} maps each pillar to the concrete platform
capabilities that operationalise it, so the framework is not left as an
abstraction but is traceable to features that this report later implements
and validates.

The pillar most under pressure from the regulatory context is \emph{retain}.
The six-month GDPR retention rule (\secref{sec:sota-gdpr}) works directly
against sustaining a language-capability pool, because the record of who
speaks what is precisely the data the rule forces a team to delete. The framework's answer is not to retain data unlawfully but to make
the most of the window the law allows: structured capture so that value is
extracted immediately, an audit trail so re-engagement is compliant and
documented, and improvement tracks so a borderline German speaker is
developed rather than lost. A natural extension of this logic --- a
consent-renewal reminder shortly before expiry, so a promising profile is
given the chance to stay in the pool rather than lapse silently --- is set out
as future work in \chapref{cap:conclusions}. This is the
precise point at which the strategic
framework and the deletion-aware data model of \chapref{cap:design}
meet.

\begin{table}[H]
\centering
\caption[Language talent framework pillars mapped to platform capabilities]{The
three framework pillars mapped to the TalentHub capabilities that
operationalise them.}
\label{tab:framework-pillars}
\small
\begin{tabular}{p{0.16\linewidth}p{0.40\linewidth}p{0.34\linewidth}}
\toprule
\textbf{Pillar} & \textbf{Framework intent} & \textbf{Operationalised by} \\
\midrule
\textbf{Attract} & Source and recognise candidates who hold the critical language profiles, and target outreach where the gaps are & CV parsing that tags languages; job-anchored matching against the per-area profile; ambassador programs; preference-aware campaigns; analytics on language supply \\
\textbf{Develop} & Verify real proficiency objectively and grow borderline talent rather than discarding it & CEFR assessment with grammar/vocabulary/fluency sub-scores; borderline flagging; improvement tracks; longitudinal assessment history \\
\textbf{Retain} & Keep a persistent, compliant view of capability and re-engage promising profiles over time & Deletion-aware talent pool; interaction-history audit trail; re-engagement campaigns within the GDPR window; status tracking across cycles \\
\bottomrule
\end{tabular}
\end{table}

\subsection{From Framework to Requirements}
\label{sec:framework-bridge}

The framework closes the gap between the strategic brief and the engineering
that follows. Each pillar generates concrete demands --- recognise languages
automatically, score them against a role, verify them objectively, develop
and re-engage compliantly --- that echo the validated pain points of
\secref{sec:analysis-pains} and crystallise into the functional requirements
of the next section.

%----------------------------------------------------------------------------------------

\section{Functional Requirements}
\label{sec:analysis-fr}

The functional requirements were captured from the client brief and refined
across sprints; several emerged only once the platform was in real use.
\tabref{tab:functional-requirements} lists them grouped by the module
that satisfies them, together with a \gls{moscow} priority and the delivery
status at submission. The grouping mirrors the architecture of
\chapref{cap:design}, so that each requirement family corresponds to a
coherent slice of the system.

\begin{table}[H]
\centering
\caption[Functional requirements]{Functional requirements grouped by module,
with \gls{moscow} priority and delivery status.}
\label{tab:functional-requirements}
\small
\begin{tabular}{p{0.07\linewidth}p{0.58\linewidth}p{0.13\linewidth}p{0.10\linewidth}}
\toprule
\textbf{ID} & \textbf{Requirement} & \textbf{Priority} & \textbf{Status} \\
\midrule
\multicolumn{4}{l}{\textit{Talent Pool}} \\
FR-02 & Persistent, searchable talent pool across campaigns & Must & Done \\
FR-07 & Deduplication of candidate records & Must & Done \\
FR-08 & CSV export of candidate data & Should & Done \\
\midrule
\multicolumn{4}{l}{\textit{CV Parsing}} \\
FR-01 & Bulk CV upload with automated structured parsing & Must & Done \\
\midrule
\multicolumn{4}{l}{\textit{Jobs and Matching}} \\
FR-04 & Job configuration with language/experience requirements & Must & Done \\
FR-05 & Automated candidate-to-job matching engine & Must & Done \\
FR-03 & Deterministic, explainable candidate scoring & Must & Done \\
FR-10 & Borderline candidate identification & Should & Done \\
FR-15 & Per-job shortlist of actively considered candidates & Should & Done \\
FR-18 & adidas careers job and internship synchronisation (scraper) & Should & Done \\
\midrule
\multicolumn{4}{l}{\textit{Assessment}} \\
FR-06 & CEFR-based language assessment & Must & Done \\
FR-16 & Per-skill verification & Should & Done \\
FR-11 & Improvement tracks for borderline candidates & Could & Partial \\
\midrule
\multicolumn{4}{l}{\textit{Notifications and Campaigns}} \\
FR-09 & HR notification system for pipeline events & Should & Done \\
FR-19 & Promotional campaigns to targeted candidate segments & Should & Done \\
FR-20 & Candidate contact with interaction-history audit trail & Should & Done \\
\midrule
\multicolumn{4}{l}{\textit{Candidate Self-Service}} \\
FR-21 & Candidate registration, self-initiated or HR-invited & Must & Done \\
FR-22 & Upload and edit CV, motivation letter and learning agreement & Should & Done \\
FR-23 & Apply to jobs, internships and ambassador programs & Must & Done \\
FR-24 & View own application and assessment status & Should & Done \\
FR-25 & Manage personal data and notification preferences & Should & Done \\
FR-26 & Account and data deletion (GDPR right to erasure) & Must & Done \\
\midrule
\multicolumn{4}{l}{\textit{Analytics}} \\
FR-12 & Analytics dashboard with recruitment metrics & Could & Done \\
\midrule
\multicolumn{4}{l}{\textit{Auth and Roles}} \\
FR-17 & Authentication and role-based access control & Must & Done \\
\bottomrule
\end{tabular}
\end{table}

A small number of requirements were deliberately deferred: bias-detection
(FR-13) and internal-mobility matching (FR-14) were scoped out of the first
version, and a PDF export (part of FR-08) and the full improvement-track
workflow (FR-11) remain partial. These choices, and the reasoning behind
them, are revisited in the limitations of \chapref{cap:conclusions}.

\subsection{Detailed Specification of Core Requirements}
\label{sec:analysis-fr-detail}

The table above is intentionally a summary; the \emph{Must}-priority
requirements that form the core of the product warrant the fuller treatment
given here, each stated with its rationale, the principal actor and the
acceptance criteria against which it was signed off during client review.

\paragraph{FR-01 --- Bulk CV upload with automated structured parsing.}
\begin{description}[noitemsep]
	\item[Actor:] HR recruiter (and, for single uploads, the candidate).
	\item[Rationale:] pain point \#5 --- recruiters spend significant time extracting basic facts from each CV.
	\item[Behaviour:] the user uploads one or many files (or a ZIP archive); the system extracts text, sends it to a Large Language Model, validates the structured result and persists a candidate with experiences, education, languages and skills.
	\item[Acceptance:] a batch of mixed-format PDFs and DOCX files yields one correctly populated candidate per CV; malformed model output never creates a corrupt record; a per-batch progress count is observable while parsing proceeds in the background; and any per-file failures are surfaced to the user with a per-file error log, without aborting the rest of the batch.
\end{description}

\paragraph{FR-02 --- Persistent, searchable talent pool.}
\begin{description}[noitemsep]
	\item[Actor:] HR recruiter.
	\item[Rationale:] pain points \#1 and \#7 --- candidate data lives in spreadsheets and cannot be sliced.
	\item[Behaviour:] every parsed or manually created candidate is stored in a relational pool that can be filtered by status, country, score range, language and source type, and sorted.
	\item[Acceptance:] a recruiter can isolate, for example, ``German-speaking candidates in Portugal with a score above 70'' in a single view without opening any document.
\end{description}

\paragraph{FR-05 / FR-03 --- Explainable job-anchored matching and scoring.}
\begin{description}[noitemsep]
	\item[Actor:] HR recruiter.
	\item[Rationale:] pain point \#3 and the \emph{attract} pillar --- candidates must be ranked against a specific role, defensibly.
	\item[Behaviour:] for a chosen job, the system computes a fit score per candidate from seven criteria and exposes a per-criterion breakdown and an eligibility flag.
	\item[Acceptance:] every ranking can be explained criterion by criterion; the same inputs always produce the same score; and a criterion the job does not specify is excluded rather than penalised.
\end{description}

\paragraph{FR-06 --- CEFR-based language assessment.}
\begin{description}[noitemsep]
	\item[Actor:] candidate (invited), HR recruiter (reviewer).
	\item[Rationale:] the \emph{develop} pillar and pain point \#3 --- self-declared proficiency is not trustworthy.
	\item[Behaviour:] an invited candidate completes an AI-mediated assessment that returns a \gls{cefr} level with sub-scores, stored for human review.
	\item[Acceptance:] the result is reproducible in format, never auto-rejects, and is recorded against the candidate's longitudinal history.
\end{description}

\paragraph{FR-09 --- HR communication with audit trail.}
\begin{description}[noitemsep]
	\item[Actor:] HR recruiter (sender), candidate (recipient).
	\item[Rationale:] pain points \#4 and \#9 and the \emph{retain} pillar.
	\item[Behaviour:] status changes, contact emails and campaigns are recorded as typed notifications and reconstructed as a per-candidate interaction timeline.
	\item[Acceptance:] any team member can retrieve the complete interaction history for a candidate, including who sent what and when.
\end{description}

\paragraph{FR-17 --- Authentication and role-based access control.}
\begin{description}[noitemsep]
	\item[Actor:] all users.
	\item[Rationale:] emerged once the candidate portal and HR dashboard had to coexist.
	\item[Behaviour:] Google \gls{oauth} authentication with an immutable server-set role claim gates HR-only operations.
	\item[Acceptance:] an unauthenticated API call is rejected with \texttt{401} (\emph{Unauthorized} --- no valid session) and a non-HR call to an HR-only route with \texttt{403} (\emph{Forbidden} --- authenticated but insufficient role).
\end{description}

%----------------------------------------------------------------------------------------

\section{Use Cases}
\label{sec:analysis-usecases}

The functional requirements are exercised through a small number of actors
and a larger set of use cases. Two human actors --- the \textbf{HR recruiter}
and the \textbf{candidate} --- drive the system, supported by two external
actors that the system depends on but does not control: the \textbf{LLM
provider} (CV and job-description parsing) and the \textbf{AI Interviewer}
sidecar (conversational assessment). The HR recruiter manages the full
recruitment pipeline --- ingesting and ranking candidates, shortlisting and
contacting them, running promotional campaigns, and keeping an audit trail of
every interaction --- while the candidate drives a complete self-service
journey, from registration (self-initiated or HR-invited) through document
management, applications to jobs, internships and the ambassador program,
assessments, and the exercise of data-protection rights such as account
deletion. \figref{fig:usecase} shows the principal use cases grouped by
actor.

\begin{figure}[ht]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
	usecase/.style={draw, ellipse, align=center, font=\scriptsize, minimum width=2.9cm, minimum height=0.78cm, fill=lstback, inner sep=1pt},
	actor/.style={font=\scriptsize\bfseries, align=center},
	system/.style={draw, dashed, rounded corners, inner sep=10pt},
	>=Stealth,
	actorpic/.pic={
		\draw[thick] (0,0.32) circle (0.12);
		\draw[thick] (0,0.20) -- (0,-0.18);
		\draw[thick] (-0.18,0.02) -- (0.18,0.02);
		\draw[thick] (0,-0.18) -- (-0.15,-0.45);
		\draw[thick] (0,-0.18) -- (0.15,-0.45);
	}
]
% Human actors
\pic at (-6.6,1.1) {actorpic}; \node[actor] (hr) at (-6.6,0.4) {HR\\Recruiter};
\pic at (6.6,0.5) {actorpic};  \node[actor] (cand) at (6.6,-0.2) {Candidate};
% External system actors
\pic at (-6.6,-3.3) {actorpic}; \node[actor] (llm) at (-6.6,-4.0) {LLM\\Provider};
\pic at (6.6,-4.4) {actorpic};  \node[actor] (ai) at (6.6,-5.1) {AI\\Interviewer};

% HR use cases (left column)
\node[usecase] (u1) at (-2.5,3.3)  {Upload \& parse CVs};
\node[usecase] (u2) at (-2.5,2.35) {Browse / filter pool};
\node[usecase] (u3) at (-2.5,1.4)  {Rank candidates for a job};
\node[usecase] (u4) at (-2.5,0.45) {Shortlist \& contact};
\node[usecase] (u5) at (-2.5,-0.5) {Run \& promote campaigns};
\node[usecase] (u6) at (-2.5,-1.45){Track interaction history};
\node[usecase] (u7) at (-2.5,-2.4) {View analytics};

% Candidate use cases (right column)
\node[usecase] (c1) at (2.5,3.3)   {Register / accept invite};
\node[usecase] (c2) at (2.5,2.45)  {Upload \& edit documents};
\node[usecase] (c3) at (2.5,1.6)   {Apply to job / internship};
\node[usecase] (c4) at (2.5,0.75)  {Apply to ambassador program};
\node[usecase] (c5) at (2.5,-0.1)  {Take assessment};
\node[usecase] (c6) at (2.5,-0.95) {View application status};
\node[usecase] (c7) at (2.5,-1.8)  {Manage profile \& preferences};
\node[usecase] (c8) at (2.5,-2.65) {Browse open positions};
\node[usecase] (c9) at (2.5,-3.5)  {Delete account (GDPR)};

% System boundary
\begin{scope}[on background layer]
\node[system, fit=(u1)(u7)(c1)(c9)] (sys) {};
\end{scope}
\node[anchor=south, font=\footnotesize\itshape] at (sys.north) {TalentHub};

% Associations
\draw (hr) -- (u1.west); \draw (hr) -- (u2.west); \draw (hr) -- (u3.west);
\draw (hr) -- (u4.west); \draw (hr) -- (u5.west); \draw (hr) -- (u6.west);
\draw (hr) -- (u7.west);
\draw (cand) -- (c1.east); \draw (cand) -- (c2.east); \draw (cand) -- (c3.east);
\draw (cand) -- (c4.east); \draw (cand) -- (c5.east); \draw (cand) -- (c6.east);
\draw (cand) -- (c7.east); \draw (cand) -- (c8.east); \draw (cand) -- (c9.east);
\draw (llm) -- (u1.south west);
\draw (ai) -- (c5.south east);
\end{tikzpicture}}
\caption[Principal use cases by actor]{Principal use cases grouped by actor.}
\label{fig:usecase}
\end{figure}

\tabref{tab:usecase-detail} expands three representative use cases into the
structured form used to drive sprint planning --- a precondition, the main
success scenario, and the principal alternative or failure path --- so that
the behaviour expected of the system is unambiguous.

\begin{table}[H]
\centering
\caption[Selected use-case descriptions]{Structured descriptions of three
representative use cases.}
\label{tab:usecase-detail}
\small
\begin{tabular}{p{0.15\linewidth}p{0.78\linewidth}}
\toprule
\multicolumn{2}{l}{\textbf{UC-1 Rank candidates for a job}} \\
\midrule
Actor & HR recruiter \\
Pre & At least one job and a populated candidate pool exist \\
Main & (1) HR opens a job's ranking screen; (2) the job description is parsed lazily into the requirements schema and cached; (3) the engine scores every candidate with \texttt{computeJobFit}; (4) results are shown ranked with a per-criterion breakdown and eligibility flag \\
Alt & If the description was parsed before and is unchanged, the cached requirements are reused (no LLM call) \\
\midrule
\multicolumn{2}{l}{\textbf{UC-2 Bulk upload and parse CVs}} \\
\midrule
Actor & HR recruiter; LLM provider (external) \\
Pre & HR is authenticated \\
Main & (1) HR uploads files or a ZIP; (2) the route returns \texttt{202} with a job id; (3) each CV is extracted, parsed and validated in the background; (4) candidates are upserted and de-duplicated; (5) progress counts update \\
Alt & On a parse failure the request is retried against the fallback provider; a job stuck $>$10\,min is marked failed \\
\midrule
\multicolumn{2}{l}{\textbf{UC-3 Take a language assessment}} \\
\midrule
Actor & Candidate; AI Interviewer (external) \\
Pre & The candidate holds a valid, unexpired assessment token \\
Main & (1) candidate opens the interview popup; (2) turns are exchanged with the sidecar; (3) proctoring events are logged; (4) a CEFR result with sub-scores is returned and stored for HR review \\
Alt & An expired or reused token is rejected \\
\bottomrule
\end{tabular}
\end{table}

%----------------------------------------------------------------------------------------

\section{Non-Functional Requirements}
\label{sec:analysis-nfr}

Beyond what the system does, several quality attributes constrained how it
had to be built: \emph{transparency} of every scoring and matching decision;
\emph{security} and \gls{gdpr} compliance for the personal data it holds;
\emph{reliability} through continuous integration; \emph{maintainability}
through the onion architecture's strict dependency rule and end-to-end type
safety; and the \emph{cost and portability} ceiling of running on free service
tiers and a serverless host. \tabref{tab:nfr} states each attribute against the
\gls{isoiec25010} product-quality characteristic \parencite{ISO25010} it
corresponds to, together with the concrete mechanism that delivers it and how
it is verified, so that ``quality'' is stated as something testable rather than
aspirational.

\begin{table}[H]
\centering
\caption[Non-functional requirements]{Non-functional requirements mapped to
ISO/IEC~25010 characteristics, their mechanism and their verification.}
\label{tab:nfr}
\small
\begin{tabular}{p{0.18\linewidth}p{0.20\linewidth}p{0.30\linewidth}p{0.22\linewidth}}
\toprule
\textbf{Attribute} & \textbf{ISO 25010} & \textbf{Mechanism} & \textbf{Verified by} \\
\midrule
Transparency & Functional appropriateness & Deterministic, per-criterion scoring; no auto-reject & \texttt{job-fit}, \texttt{scoring} tests \\
Security & Security & Server-side authz, deletion-aware model, PII redaction & Auth + redaction suites; review \\
Reliability & Reliability & Provider fallback; CI type-check + tests on every push & GitHub Actions; 527 tests \\
Maintainability & Maintainability & Onion architecture; strict TypeScript; one composition root & \texttt{tsc -{}-noEmit}; layer mocks \\
Portability \& cost & Portability & Free-tier managed services; serverless deploy & Production deployment \\
Usability & Usability & Role-based IA; accessible shadcn/ui primitives & Client acceptance sessions \\
\bottomrule
\end{tabular}
\end{table}

%----------------------------------------------------------------------------------------

\section{Project Management}
\label{sec:analysis-pm}

The BlendEd program ran on Scrum, with a Scrum Master facilitating shared
ceremonies across its four subproject teams over the February--July~2026
internship. This section focuses on how that framework played out for the
two-person talent-pool subproject. Acting as the subproject's Product Owner,
the author owned the product vision, the backlog and its prioritisation, and
the relationship with the client; both members acted as developers. The
section describes how the framework was adapted at subproject scale and how
the backlog was ordered.

\subsection{Scrum at Program and Subproject Scale}
\label{sec:method-scrum}

By-the-book Scrum assigns three distinct accountabilities --- Product Owner,
Scrum Master and Developers \parencite{ScrumGuide2020,SchwaberBeedle2002}. At
program level all three were present: a Scrum
Master facilitated the shared ceremonies, and each of the four subprojects
had its own Product Owner. Within the two-person talent-pool sub-team those
accountabilities collapsed onto two people, and the adaptation kept the parts
that add value at that scale:

\begin{itemize}
	\item \textbf{Roles.} The author held the Product Owner
	accountability for the talent pool: gathering requirements from the
	client, maintaining the backlog, deciding priority, and accepting or
	rejecting completed work. Both members were Developers. The Scrum Master
	role sat at program level --- one person for the first two sprints and a
	second for the final two --- so the sub-team carried no dedicated Scrum
	Master of its own and kept internal facilitation lightweight.
	\item \textbf{Cadence.} Work was organised in short iterations aligned
	with the academic calendar and the program's sprint boundaries, each
	delivering a demonstrable increment of the deployed application
	\parencite{BeckAgileManifesto2001}.
	\item \textbf{Ceremonies.} Beyond the program-level sprint
	\emph{planning}, \emph{review} and \emph{retrospective} run with the
	Scrum Master across all four subprojects, the talent-pool team kept its
		own lightweight rhythm: an informal \emph{weekly} sync kept the two
	developers aligned, and a dedicated client-acceptance session
	(\secref{sec:val-manual}) doubled as the subproject's sprint
	review, so that the people who reported the pain points also signed off
	the increments that addressed them.
	\item \textbf{Tooling.} The product increment was always the live
	Vercel deployment. Source control on GitHub, a conventional-commit
	history (\texttt{feat}, \texttt{fix}, \texttt{docs}, \texttt{refactor})
	and continuous integration gave each increment an auditable trail, and
	the meeting minutes (\appref{app:survey}) record the ceremonies.
	Communication across the four subproject teams was handled through
	Discord (structured channels per topic) and WhatsApp (informal
	coordination), while a shared Figma board served as the programme's
	living knowledge base --- storing meeting notes, brainstorming
	artefacts, architectural decisions and cross-team reference material
	in a single space accessible to all participants regardless of
	subproject.
\end{itemize}

The clearest benefit of the adaptation was the fusion of sprint review with
client acceptance: because the client validated each increment directly
against the originating pain point, the feedback loop that Scrum prescribes
was both real and short. The clearest tension --- one person holding both
the Product Owner and lead-developer roles --- is examined as a lesson in
\chapref{cap:conclusions}.

\subsection{Planning and Timeline}
\label{sec:method-timeline}

The five-month internship (16~February~--~9~July~2026) was organised into
seven overlapping work streams aligned with the academic calendar.
\figref{fig:gantt} shows the timeline as actually executed: analysis and
architecture front-loaded into the first weeks, a long core-development
stream through spring, the language-assessment integration layered on top in
April, and validation, the survey and report writing closing the project.
The overlaps are deliberate --- testing and deployment ran continuously
alongside development rather than as a final phase --- which is what the
continuous-integration discipline of \chapref{cap:validation} made
possible.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=1.55cm,y=0.62cm]
% Month axis (Feb=0 ... Jun=4, Jul=5)
\foreach \x/\m in {0/Feb,1/Mar,2/Apr,3/May,4/Jun,5/Jul} {
  \draw[lstcomment!40] (\x,0.4) -- (\x,-7.2);
  \node[font=\scriptsize\bfseries] at (\x,0.9) {\m};
}
% Task bars: \barrow{row}{start}{end}{label}
\newcommand{\barrow}[4]{%
  \fill[lstkeyword!75, rounded corners=2pt] (#2,-#1+0.28) rectangle (#3,-#1-0.28);
  \node[anchor=west, font=\scriptsize] at (5.15,-#1) {#4};
}
\barrow{1}{0}{0.7}{Analysis \& requirements}
\barrow{2}{0.4}{1.6}{Architecture \& DB design}
\barrow{3}{1.4}{3.2}{Core development (parsing, pool, matching)}
\barrow{4}{2.4}{3.2}{Language-assessment integration}
\barrow{5}{1.6}{4.4}{Testing, deployment \& validation}
\barrow{6}{3.4}{4.2}{Survey design, collection \& analysis}
\barrow{7}{3.6}{5.0}{Final report \& presentation}
% Situation-point marker
\draw[lststring, thick, dashed] (2.95,0.4) -- (2.95,-7.2);
\node[lststring, font=\scriptsize, anchor=south, rotate=90] at (2.95,-7.2) {Situation point (end Apr)};
\end{tikzpicture}
\caption[Project timeline]{Project timeline as executed (February--July
2026).}
\label{fig:gantt}
\end{figure}

\subsection{Product Backlog and Prioritisation}
\label{sec:method-backlog}

The backlog was kept deliberately client-driven
\parencite{Cohn2004UserStories}: every item traced back to a
pain point, and priority was assigned with \textbf{MoSCoW}
\parencite{DSDMMoSCoW}
(Must / Should / Could / Won't) as shown in
\tabref{tab:functional-requirements}. The \emph{Must} items formed the
core that had to ship for the platform to be useful at all --- the talent
pool, CV parsing, job-anchored matching and the CEFR assessment --- and they
map onto the four priority pain points (\#5, \#3, \#9, \#2) identified in
\secref{sec:analysis-pains}.

Ordering was also constrained by dependencies between modules rather than by
priority alone. The talent pool and the candidate data model had to exist
before CV parsing could populate them; CV parsing had to produce
field-of-work tags before job-anchored matching could consume them; and
matching had to exist before shortlisting and analytics could build on its
output. The backlog therefore advanced in dependency order within each
priority band, which is why some \emph{Should} and \emph{Could} items (for
example analytics) were delivered only once their \emph{Must} prerequisites
were in place. Requirements that emerged during development --- notably
authentication and role-based access control (FR-17) and per-skill
verification (FR-16) --- were folded into the backlog and re-prioritised in
the same way rather than handled out of band.

\subsection{Module Boundary with the AI Interviewer}
\label{sec:method-boundary}

The single most important organisational decision was where to draw the line
between the two developers. The conversational AI Interviewer was carved out
as a separate Python \textit{FastAPI} service behind a narrow, explicit
contract to the Next.js core, the technical detail of which is given in
\chapref{cap:design}. What matters here is the organisational payoff: because
that interface was small and well defined, the two members could work in
parallel with minimal coordination, and the sidecar could be developed,
deployed and tested independently --- the clean seam that made the onion
architecture pay off in practice.

\subsection{Risks and Decision Log}
\label{sec:method-risks}

The main risks were tracked and mitigated rather than left implicit. The
dependence on a hosted \gls{llm} was mitigated by a provider fallback. The risk of \gls{gdpr} misalignment was mitigated by a
deletion-aware data model and by storing only the inferences actually
needed. The risk of \textbf{scope creep} --- amplified by the combined Product Owner
and lead-developer role, examined as a lesson in \secref{sec:concl-po} --- was
contained by the MoSCoW discipline (\secref{sec:method-backlog}) and by
deferring whole feature families (bias detection, internal mobility)
explicitly rather than silently. Finally, the risk of
integration drift between core and sidecar was contained by the small, typed
token contract described above.

\tabref{tab:risk-register} records these and the other tracked risks as a
register, rating each by likelihood and impact (Low/Medium/High) before
mitigation and naming the response. The register was reviewed at each sprint
boundary; no risk on it materialised into a project-stopping event, which is
itself evidence that the mitigations were chosen early enough.

\begin{table}[ht]
\centering
\caption[Risk register]{Project risk register with pre-mitigation likelihood
and impact, and the mitigation applied.}
\label{tab:risk-register}
\small
\begin{tabular}{p{0.27\linewidth}cc p{0.42\linewidth}}
\toprule
\textbf{Risk} & \textbf{L} & \textbf{I} & \textbf{Mitigation} \\
\midrule
LLM provider outage or rate limit & H & H & Groq$\rightarrow$OpenAI fallback; lazy JD parsing; per-call telemetry \\
Scope creep (PO = lead dev) & H & M & MoSCoW discipline; explicit deferral of feature families \\
Core$\leftrightarrow$sidecar integration drift & M & H & Small typed HMAC token contract; shared session scheme \\
GDPR misalignment & M & H & Deletion-aware model; minimal inference storage; audit trail \\
Unreliable LLM-parsed data & H & M & Strict tolerant Zod schemas; single retry; dedup tiering \\
Leaked credential in history & M & H & Review, key rotation, removal from migration scripts \\
Single-developer bus factor & M & M & Onion boundaries; documented contract; conventional commits \\
Browser speech-API limits & H & L & Documented Chromium constraint; future Whisper fallback \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Deviations from the Initial Plan}
\label{sec:method-deviations}

Two deviations from the initial proposal are worth recording, both minor and
both absorbed without disrupting the backlog; both were surfaced and accepted
at the end-of-April~2026 situation-point review with the client. First, the
persistence layer was migrated from the originally planned Prisma ORM over a
Neon PostgreSQL database to \textbf{Supabase}. The driver was integration
economy: adopting Supabase folded the database, file storage and ---
decisively --- authentication (Google OAuth) into a single managed platform,
removing the need to build and secure a separate authentication stack within
the academic timeframe. Second, a public \textbf{welcome page} was added to
the deployed application at the explicit request of the adidas Design team,
providing a branded entry point for visitors. Neither change altered the
scope agreed in sprint zero --- a talent pool with communication
verification, deliberately \emph{not} a full \gls{ats} --- and both were
folded into the backlog and re-prioritised in the same way as any
emergent requirement.

%----------------------------------------------------------------------------------------

\section{Conclusion}
\label{sec:analysis-conclusion}

The analysis produced a prioritised, traceable set of requirements anchored
in validated client pain points, together with the quality attributes that
constrain the solution and the Scrum-based method used to deliver it. The
next chapter translates these requirements into a concrete software design.
