\chapter{Verification and Validation}
\label{cap:validation}

This chapter reports how TalentHub was checked for correctness
(\textit{verification} --- ``are we building the product right?'') and how
it was confirmed to address the problems identified during discovery
(\textit{validation} --- ``are we building the right product?''). The
verification effort rests on three complementary layers: static analysis
through the TypeScript compiler, an automated unit-test suite executed on
every change, and manual acceptance testing with the client. Validation is
addressed through a structured survey mapped one-to-one onto the pain points
of \chapref{cap:introduction}, and the chapter closes with the security
review that accompanied the project.

%----------------------------------------------------------------------------------------

\section{Verification Strategy}
\label{sec:val-verification}

Verification follows the same boundaries as the \gls{onion} described in
\chapref{cap:design}. Each layer is checked with the technique best
suited to it: the type system guarantees structural correctness across all
layers; pure Domain and Application logic is exercised by fast, isolated
unit tests; and the Presentation boundary (authentication and route gating)
is tested through request-level assertions. Because every external
dependency is reached through a port, the suite mocks at those interfaces
and never touches the network or the database, which keeps the whole run
deterministic and fast enough to execute on every push.

\subsection{Static Analysis and Type Safety}
\label{sec:val-static}

The first line of defence is the TypeScript compiler running in
\texttt{strict} mode. The command \texttt{tsc -{}-noEmit} type-checks the
whole codebase without producing output and is the canonical local
validation step for this project, in preference to a full production build
(the build intentionally fails locally because it depends on
deployment-only environment variables for the interview routes). Strict
mode forbids implicit \texttt{any}, enforces null-safety, and makes the
path aliases (\texttt{@server}, \texttt{@client}, \texttt{@/lib}) resolve
identically in source and in tests.

A review of the type guarantees found that their weakest point was the
database boundary, where rows were cast through
\texttt{camelizeKeys<any>} and lost their types exactly where data crosses
from PostgreSQL into the Domain. This observation is recorded as a
maintainability item; it illustrates that type checking is necessary but
not sufficient, and motivates the unit tests that pin the behaviour of the
conversion utilities and the schema validators.

\subsection{Unit Tests}
\label{sec:val-unit}

\subsubsection{Tooling}

The suite runs on \textbf{Vitest~4.1.5} with the V8 coverage provider. Vitest
was chosen over Jest because it natively supports ECMAScript modules (no
CommonJS transform step), reuses the same path aliases as the application
through its Vite resolver, and starts quickly thanks to the esbuild
transformer. Tests live in the top-level \texttt{tests/} directory and
alongside the code under \texttt{src/}, and are executed with
\texttt{vitest run} (single pass, used in CI) or \texttt{vitest -{}-watch}
during development. Coverage is produced with \texttt{vitest run
-{}-coverage} and uploaded as a continuous-integration artefact.

\subsubsection{Suite Composition}

At the time of writing the suite comprises \textbf{33 test files and 527
test cases, all passing}. \tabref{tab:test-inventory} lists every file
with its case count, the application layer it targets, and the concern it
covers. The suite deliberately concentrates on the layers where
consequential logic lives --- the Domain (pure decision functions), the
Application (orchestration, boundary validation, and data-transfer
transforms), and the server-side authorisation guards --- while the thin
Infrastructure adapters (data repositories and external-service clients) are
verified indirectly through the use cases that depend on them.

\begin{table}[ht]
\centering
\caption[Automated test inventory]{Automated test inventory at submission
(33 files, 527 cases, all passing), sorted by case count.}
\label{tab:test-inventory}
\footnotesize
\begin{tabular}{p{0.32\linewidth}r p{0.16\linewidth}p{0.32\linewidth}}
\toprule
\textbf{File} & \textbf{Cases} & \textbf{Layer} & \textbf{Concern} \\
\midrule
\texttt{job-fit} & 47 & Domain & Job-anchored matching engine \\
\texttt{middleware-auth} & 38 & Presentation & Route gating (401/403) \\
\texttt{upload-use-cases} & 38 & Application & CV upload + parsing-job pipeline \\
\texttt{interview-runtime} & 37 & App + Infra & Interview rubric + evidence \\
\texttt{dtos} & 31 & Application & DTO transforms + request schemas \\
\texttt{job-use-cases} & 31 & Application & Job CRUD, sync, matching \\
\texttt{notification-use-cases} & 28 & Application & Notifications + campaigns \\
\texttt{candidate-use-cases} & 26 & Application & Candidate list / update / notify \\
\texttt{job-matching-bridge} & 25 & Application & DB-row $\rightarrow$ matcher bridge \\
\texttt{cv-validation} & 19 & Application & CV extraction schema \\
\texttt{analytics-catalog} & 16 & Domain & Widget-spec + injection guard \\
\texttt{notifications-route-auth} & 16 & Presentation & API route authorisation \\
\texttt{db-utils} & 14 & Infrastructure & snake/camel + JSONB conversion \\
\texttt{scoring} & 13 & Domain & Scoring + CEFR estimation \\
\texttt{ambassador-use-cases} & 12 & Application & Ambassador programs \\
\texttt{profile-use-cases} & 12 & Application & Candidate profile resolution \\
\texttt{dashboard-widget-use-cases} & 11 & Application & Custom analytics widgets \\
\texttt{adidas-job-scraper} & 10 & Application & Job-listing scraper \\
\texttt{logger-redaction} & 10 & Infrastructure & PII redaction in logs \\
\texttt{text-extraction} & 10 & Infrastructure & PDF/DOCX extraction \\
\texttt{escape-or-term} & 9 & Infrastructure & PostgREST \texttt{.or()} escaping \\
\texttt{job-requirements-schema} & 9 & Application & JD extractor schema \\
\texttt{listing-posted-date} & 9 & Domain & Fuzzy posted-date parsing \\
\texttt{job-shortlist-use-cases} & 8 & Application & Per-job shortlist \\
\texttt{segment-use-cases} & 8 & Application & Candidate segments \\
\texttt{require-hr} & 7 & Presentation & HR-only route guard \\
\texttt{interview-token} & 6 & Infrastructure & HMAC token round-trip \\
\texttt{application-use-cases} & 5 & Application & Apply / withdraw / status \\
\texttt{cv-fields-of-work} & 5 & Application & Field-of-work tagging \\
\texttt{hr-profile-analytics-use-cases} & 5 & Application & HR profile + analytics \\
\texttt{resolve-caller} & 5 & Presentation & Server auth gate \\
\texttt{assessment-use-cases} & 4 & Application & Assessment invite + link \\
\texttt{export-use-cases} & 3 & Application & CSV export mapping \\
\midrule
\textbf{Total} & \textbf{527} & & \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Coverage by Layer}

Case counts show \emph{how much} was tested; they do not show \emph{where}
the testing landed. \tabref{tab:coverage-layer} and
\figref{fig:coverage-by-layer} report the statement, branch, function, and
line coverage measured by the V8 provider, broken down by architectural
layer. The distribution is the quantitative expression of the
risk-proportional strategy: every layer that holds decision logic --- the
domain services, the application use cases and their data-transfer schemas,
and the authorisation guards --- is covered between roughly 80\% and 100\%
of its lines, whereas the thin infrastructure adapters (data repositories and
external-service clients, which contain little branching and are reached only
through mocked ports) are left deliberately low. The whole-codebase line
figure of 46.9\% is therefore not a uniform average but a blend of
near-exhaustive coverage where a defect would be costly and near-zero
coverage where a unit test would merely restate a database query.

\begin{table}[ht]
\centering
\caption[Coverage by architectural layer]{Coverage by architectural layer
(V8 provider, percentages). Decision-bearing layers are covered heavily;
thin infrastructure adapters are covered indirectly by design.}
\label{tab:coverage-layer}
\small
\begin{tabular}{p{0.44\linewidth}rrrr}
\toprule
\textbf{Layer} & \textbf{Stmts} & \textbf{Branch} & \textbf{Funcs} & \textbf{Lines} \\
\midrule
Presentation --- authorisation guards & 100 & 100 & 100 & \textbf{100} \\
Domain services (matching, scoring) & 91.0 & 76.1 & 98.3 & \textbf{96.2} \\
Application --- use cases & 84.4 & 73.1 & 80.1 & \textbf{85.7} \\
Application --- DTOs \& errors & 79.1 & 86.5 & 85.7 & \textbf{80.5} \\
Infrastructure --- security (token) & 89.3 & 89.5 & 87.5 & 89.3 \\
Infrastructure --- logging & 71.4 & 64.5 & 72.7 & 74.5 \\
Infrastructure --- text extraction & 73.9 & 62.5 & 80.0 & 73.9 \\
Infrastructure --- scraping & 35.1 & 31.0 & 25.0 & 33.1 \\
Infrastructure --- data repositories & 2.5 & 3.1 & 4.3 & 2.7 \\
\midrule
\textbf{Whole codebase} & \textbf{45.5} & \textbf{43.3} & \textbf{42.5} & \textbf{46.9} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.92\linewidth, height=7.8cm,
  xbar, bar width=9pt,
  xmin=0, xmax=115,
  xlabel={Line coverage (\%)},
  symbolic y coords={Data repositories,Scraping,Text extraction,Logging,DTOs \& errors,Application use cases,Security token,Domain services,Auth guards},
  ytick=data, y tick label style={font=\footnotesize},
  nodes near coords, nodes near coords style={font=\scriptsize},
  every node near coord/.append style={anchor=west},
  enlarge y limits=0.08,
  xmajorgrids, tick align=inside,
  ]
\addplot[fill=lstkeyword!60, draw=lstkeyword] coordinates {
  (2.7,Data repositories)
  (33.1,Scraping)
  (73.9,Text extraction)
  (74.5,Logging)
  (80.5,DTOs \& errors)
  (85.7,Application use cases)
  (89.3,Security token)
  (96.2,Domain services)
  (100,Auth guards)
};
\end{axis}
\end{tikzpicture}
\caption[Line coverage by architectural layer]{Line coverage by layer
(V8 provider). The decision-bearing layers --- domain services, application
use cases and their DTO schemas, and the authorisation guards --- sit between
80\% and 100\%, while the thin infrastructure adapters are covered only
indirectly. The whole-codebase line average is 46.9\%.}
\label{fig:coverage-by-layer}
\end{figure}

\subsubsection{What is Tested, and Why}

The suite is not uniform by design; the depth of testing in each area is
proportional to its risk and to how much consequential logic it contains.

\paragraph{Decision-critical Domain logic (highest priority).}
The job-anchored matching engine (\texttt{computeJobFit}) is the most
heavily tested unit, with 47 cases. Being a pure function with no external
dependencies, it is straightforward to exercise exhaustively: the tests cover
every one of the seven criteria
(field, experience-in-field, seniority, required and preferred skills,
languages, education), the ``average of applicable criteria only'' rule,
and the \texttt{isEligible} flag computed as the conjunction of applicable
\texttt{met} flags. A dedicated \textit{golden skill-match corpus} locks in
expected match and no-match outcomes for realistic skill pairs --- for
example that \texttt{js} matches \texttt{javascript} and \texttt{k8s}
matches \texttt{kubernetes} through curated synonym groups and phrase
aliases, while genuinely distinct products such as AWS and Azure, or Java
and JavaScript, are kept apart. This corpus exists specifically to prevent
silent regressions in matching accuracy when the synonym tables are edited.
The scoring service (13 cases) is tested in the same spirit, covering score
ranges, relative ordering, boundary capping, and the keyword-to-\gls{cefr}
mapping.

\paragraph{The glue between layers.}
The \texttt{job-matching-bridge} suite (25 cases) was added after a
production defect in which the bridge that converts a database row into the
matcher's input read the wrong property (\texttt{title} instead of
\texttt{jobTitle}), silently emptying the evidence text that supports
title-based seniority matching. The pure matcher was already well tested,
yet the untested ``glue'' in the Application layer carried the bug. The
new suite tests the loose-date parser, the experience-duration calculation,
the requirement-building helper, and includes an explicit regression case
asserting that the job title is carried through. The lesson --- that pure
logic and the code wiring it together must both be tested --- is revisited
in \chapref{cap:conclusions}.

\paragraph{Data-integrity boundaries.}
The platform ingests data from Large Language Models, whose output cannot be
trusted structurally. Four schema suites (\texttt{cv-validation},
\texttt{job-requirements-schema}, \texttt{cv-fields-of-work}, and the
\texttt{analytics-catalog} spec validation) assert that
Zod schemas accept well-formed payloads, reject missing required fields,
normalise values such as URLs and e-mail addresses, and tolerantly discard
model-invented values that fall outside the canonical vocabulary (for
instance, fields of work outside the sixteen allowed).

\paragraph{Security-sensitive paths.}
Several suites exist purely to defend security and compliance properties:
\texttt{middleware-auth} (38 cases) and \texttt{notifications-route-auth}
(16 cases) assert that unauthenticated callers receive \texttt{401} and
non-HR callers receive \texttt{403} on protected routes; \texttt{escape-or-term}
(9 cases) verifies that user search terms are escaped before reaching
PostgREST \texttt{.or()} filters, closing an injection vector found during a
security review; \texttt{logger-redaction} (10 cases) confirms that e-mail addresses
and tokens are masked in structured logs; and the analytics catalog tests
confirm that the strict widget-specification schema rejects injected keys,
so that the constrained chart builder can never be coerced into running an
arbitrary query.

\paragraph{Module ownership note.}
Two suites, \texttt{interview-runtime} (37 cases) and \texttt{interview-token}
(6 cases), exercise the AI Interviewer integration. The conversational
FastAPI service itself is the second team member's (\secref{sec:team}); these
tests cover only the Next.js side of that integration --- session creation,
HMAC token signing and expiry, transcript-turn persistence, and the
rubric-validation guardrails applied before an evaluation is stored.

\subsubsection{Requirements Traceability}

Verification is only meaningful if it can be traced back to what the system
was supposed to do. \tabref{tab:traceability} threads each principal
capability from its design home, through the automated test(s) that pin it,
to the validation evidence that confirms it mattered to the client. The
matrix is deliberately three-columned: a capability with a design but no test
is unverified, and a capability that passes tests but maps to no client need
is gold-plating --- the table makes both failure modes visible. Every
must-have capability traces to at least one automated test, and the
user-facing ones additionally trace to a survey item or an acceptance session.

\begin{table}[ht]
\centering
\caption[Requirements traceability matrix]{Traceability from capability to
automated test to validation evidence. Survey items are the numbered
questions of \appref{app:survey}.}
\label{tab:traceability}
\small
\begin{tabular}{p{0.30\linewidth}p{0.34\linewidth}p{0.26\linewidth}}
\toprule
\textbf{Capability} & \textbf{Automated test(s)} & \textbf{Validation} \\
\midrule
Job-anchored matching & \texttt{job-fit}, \texttt{job-matching-bridge} & Survey Q10, Q11 \\
CV extraction \& schema & \texttt{cv-validation}, \texttt{text-extraction}, \texttt{cv-fields-of-work} & Survey Q7, Q11 \\
CV upload pipeline & \texttt{upload-use-cases} & Acceptance session \\
Deduplication & \texttt{upload-use-cases} & Survey Q2 \\
CV / CEFR scoring & \texttt{scoring} & Survey Q4 \\
JD requirement extraction & \texttt{job-requirements-schema} & Survey Q11 \\
Per-job shortlist & \texttt{job-shortlist-use-cases} & Survey Q14 \\
Authn \& route RBAC & \texttt{middleware-auth}, \texttt{notifications-route-auth} & Security review \\
Search-injection safety & \texttt{escape-or-term} & Security review \\
PII redaction in logs & \texttt{logger-redaction} & GDPR review \\
Interview token contract & \texttt{interview-token}, \texttt{interview-runtime} & Security review \\
Constrained analytics & \texttt{analytics-catalog} & Survey Q9, Q10 \\
Interaction history & Acceptance session & Survey Q5, Q14 \\
Retention / deletion-aware model & Design + acceptance & Survey Q3 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Test Architecture and Mocking}

The tests mirror the application's layering. Because every external
dependency is reached through a Domain port, each port has a hand-written
mock factory (for example a fake candidate repository, CV parser, storage
service, and text extractor for the upload pipeline). External libraries
that have no port --- the PDF and DOCX parsers, and the Supabase client used
by the interview routes --- are replaced with module mocks. No mocking
library is used; plain stub functions with explicit return values keep each
test file self-contained, with its own fixtures and no shared global setup.
This makes failures local and easy to read, at the small cost of some
duplication between files.

\subsection{Manual Acceptance Testing with the Client}
\label{sec:val-manual}

Automated tests cannot judge whether a screen \textit{feels} faster to a
recruiter, so the automated suite was complemented by structured acceptance
sessions with the client. Working from a seeded dataset of representative
CVs and job descriptions, the team walked through the end-to-end recruiter
journey --- uploading and parsing a CV, opening a job and ranking candidates
against it, shortlisting, contacting a candidate, and reviewing analytics ---
and recorded the client's feedback against the original pain points. These
sessions were also the principal vehicle for sprint reviews, doubling as
both acceptance testing and Scrum ceremony (\secref{sec:analysis-pm}).

%----------------------------------------------------------------------------------------

\section{Validation through Client Survey}
\label{sec:val-survey}

Verification establishes that the software behaves as specified; validation
asks whether the specification actually solves the client's problem. To
answer this, the nine pain points of \tabref{tab:pain-points-overview}
were turned into a fourteen-item survey instrument, so that every reported
friction could be traced to an explicit question. The instrument was
completed by the lead HR stakeholder at adidas Porto --- the same person who
framed the original brief --- and the response is therefore best read as a
structured validation interview from the client. Its value lies in the detail
and authority of the source: this is a
mature shared-services HR function that already operates an enterprise
Applicant Tracking System (SAP SuccessFactors), which makes the friction it
still reports all the more telling. The full instrument and the verbatim
responses are reproduced in \appref{app:survey}
(\tabref{tab:survey-responses}).

Three findings stand out as direct validation of the project's premise.
First, when asked to rate, on a scale of one to five, how much current
recruitment tools help the team work more efficiently and reduce repetitive
manual work, the respondent answered \textbf{1 --- ``they add very little
value''}. That a team equipped with a leading enterprise ATS still rates
tool-driven productivity at the floor of the scale is the single strongest
motivation for a focused talent-intelligence layer: the gap is not the
absence of software but the absence of software that removes the repetitive
work. Second, the respondent estimated that \textbf{30--60\% of the CVs
received for a typical opening clearly fail the minimum requirements} ---
wrong location, missing language, insufficient experience. A first-pass,
explainable fit filter that surfaces eligibility before a human opens the
document addresses exactly this waste. Third, strategic planning of which
skills and languages to target is done by \textbf{relying on input from
managers} rather than on data from past recruitments, validating the
analytics and job-matching engine as a planning instrument, not merely an
operational one.

The remaining answers reinforce the prioritisation rather than headline it.
Cross-team coordination is \textit{``sometimes difficult --- you need to
enter each process or ask the colleague''}, the precise friction the
interaction-history panel removes. Re-engagement of promising but
non-selected candidates already happens --- profiles are kept and candidates
are sometimes informed of new openings --- but the respondent volunteered
that ``the process here has still space for improvement'', which is the
niche the campaign and improvement-track features occupy. The team also
reported \textit{occasionally} losing useful candidate information to
data-retention rules, confirming that the six-month constraint is a live
concern even where an ATS is present. Language and skill checks are already
performed both before the first interview and in a dedicated assessment
round, confirming that an early, objective screening layer fits the existing
workflow rather than disrupting it.

It is equally important to record where the client is \emph{not} acutely
pained, so the validation stays balanced. With an enterprise ATS in place,
the respondent finds it easy to get a pipeline overview, can identify repeat
applicants automatically, and reports extracting key fields from a CV in
under a minute and rarely opening an unsuitable one. These answers temper any
claim that the platform replaces an ATS; they instead confirm its actual
positioning, argued in \chapref{cap:state-of-the-art}, as a focused
talent-intelligence and language-verification layer. The validity of the
findings is bounded by a single respondent from one organisation, so the
results are read as directional confirmation that the prioritisation was
correct, not as a statistically generalisable claim.

\figref{fig:survey-classification} summarises the fourteen survey items
classified by how they bear on the project. The classification is the
author's reading of the responses, not an additional measurement: six items
describe a gap the platform is built to fill, five describe work the existing
enterprise ATS already handles adequately, and three are contextual
background. The shape of this distribution is itself the validation result
--- a clear cluster of gap-validating answers --- while the visible ATS-adequate
cluster is what keeps the platform's claimed scope realistic.

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.82\linewidth, height=5.2cm,
  ybar, bar width=26pt,
  ymin=0, ymax=8,
  ylabel={Survey items (of 14)},
  symbolic x coords={Gap validated, ATS adequate, Contextual},
  xtick=data, x tick label style={font=\small},
  nodes near coords, enlarge x limits=0.3,
  ymajorgrids, tick align=inside,
  ]
\addplot[fill=lstkeyword!60, draw=lstkeyword] coordinates {(Gap validated,6) (ATS adequate,5) (Contextual,3)};
\end{axis}
\end{tikzpicture}
\caption[Survey items by validation category]{Survey items by validation
category. Gap-validating: Q3, Q10--Q14; ATS-adequate: Q2, Q6--Q9; contextual:
Q1, Q4, Q5.}
\label{fig:survey-classification}
\end{figure}

%----------------------------------------------------------------------------------------

\section{Security Review}
\label{sec:val-security}

Security was treated as a first-class verification concern rather than an
afterthought, for two reasons: the platform stores personal
data under \gls{gdpr}, and it disables PostgreSQL \gls{rls} in
favour of enforcing every access decision in server-side code reached
through the service role. That design choice concentrates the
authorisation burden in the application, which is precisely why the
authentication and route-gating suites described above are central to the
security posture.

A focused security review of the codebase produced a prioritised findings log
that drove concrete remediation. The most serious item was a leaked service-role
credential committed in an obsolete migration script; the file was removed
and the key rotated in the Supabase dashboard, with the corresponding
environment variables updated across all deployment targets. The review also
identified --- and the team resolved --- a PostgREST \texttt{.or()} injection
vector in the search filters, fixed by introducing and unit-testing the
\texttt{escapeOrTerm} helper and applying it at every call site. Remaining
items, such as extracting the interview routes behind a use-case layer and
replacing residual \texttt{any} casts at the repository boundary with typed
row interfaces, are recorded as tracked maintainability work rather than
open vulnerabilities.

Finally, a sweep with the Supabase Security Advisor was used to confirm the
intended state of the database surface. The remaining advisor entries
correspond to deliberate design decisions --- chiefly that \gls{rls} is
disabled by design on internal tables that are never reached by an
end-user session --- and each is recorded with its rationale in a decisions
log so that an intentional choice is never mistaken for an oversight.

%----------------------------------------------------------------------------------------

\section{Performance Considerations}
\label{sec:val-performance}

Performance was addressed pragmatically, at the points the data model makes
hot. Indexes back the read paths that the recruiter workflow exercises most
often: the \texttt{job\_matches} and \texttt{job\_shortlists} tables for the
per-job ranking and shortlist screens, and a GIN index on
\texttt{experiences.fields\_of\_work} so that candidates can be filtered by
field of work without a full scan. Expensive computation is cached rather
than repeated: the lazy, schema-versioned JD parse designed in
\secref{sec:design-jd} runs the Large Language Model only on first use, and the
top-ranked candidates for a job are persisted as a \texttt{job\_matches} cache
rather than recomputed on every view.

The principal known bottleneck is the candidate fan-out inside
\texttt{matchCandidatesToJob}, which currently loads the candidate pool and
evaluates the matcher in application code. The remediation identified for this
bottleneck --- pre-filtering candidates in SQL through the fields-of-work GIN
index so that only plausibly relevant profiles reach the matcher --- is
recorded as planned work; the existing matcher tests are written so that
they continue to hold once that optimisation is introduced.

%----------------------------------------------------------------------------------------

\section{Limitations Found During Validation}
\label{sec:val-limitations}

Validation also surfaced limitations that are reported here openly rather
than papered over. The browser-based speech-to-text and text-to-speech used by the
assessment experience rely on Web Speech APIs available only in
Chromium-based browsers (Chrome and Edge), so the spoken-interaction path
degrades on other browsers. The platform also depends on \emph{external}
Large Language Model providers, reached over the network and run against a
provider free tier: this imposes both a practical cost-and-rate ceiling on
bulk CV parsing and a standing dependence on third-party availability. The
Groq-to-OpenAI fallback and the lazy parsing of job descriptions reduce this
exposure --- turning a primary-provider outage into degraded rather than
failed parsing --- but they do not remove it, since a simultaneous failure of
both providers would halt AI-assisted ingestion entirely. CV ingestion also reads text
directly from the uploaded file with no optical-character-recognition
fallback, so scanned or image-only PDFs cannot be parsed automatically and are
instead flagged for manual review. Some \gls{gdpr} obligations are
still operationally manual: candidates can erase their own account on demand,
but the time-based retention purge is not yet automated and still depends on
a manual trigger rather than a schedule. Finally, the constrained analytics builder, while safe by
construction, exposes a deliberately narrow catalogue of metrics and
dimensions, so some questions an analyst might ask cannot yet be expressed.

A further pair of limitations concerns the \emph{external} validation of the
AI-assisted ranking rather than its internal behaviour. First, while the
matching engine's logic is proven deterministic and regression-locked
(\secref{sec:val-unit}), its rankings were never compared against a recruiter's
own ordering of the same candidates --- no such agreement study was run.
Second, and separately, the accuracy of CV and job-description extraction was
verified structurally, through schema conformance and the retry-and-fallback
path, but was not quantified against a labelled ground-truth set in
precision-and-recall terms. This distinction matters because schema validation
is a \emph{structural} guarantee, not a \emph{semantic} one: it rejects
malformed or out-of-vocabulary output, but a \gls{hallucination} that happens
to be well-formed --- a plausible yet incorrect employer, date or skill that
satisfies the schema --- passes through unflagged. The residual risk is
therefore not corrupt records but silently wrong ones, which only a
ground-truth comparison or human review would catch, and which the platform
deliberately keeps under human review rather than acting on automatically.
Both gaps share a single root cause: the project
had access only to the job descriptions scraped from the adidas careers site,
not to real application data, and recruiter availability for hand-ranking
exercises was reduced in the closing phase. They are therefore carried forward
as validation work for a later project phase (\secref{sec:concl-future}) rather
than claimed here.

These items frame the future work discussed in
\chapref{cap:conclusions}.
